build:
  config:
    PORT: 80
    REACT_APP_BACKEND_PATH: https://brunobilucaportaladmin.herokuapp.com
    REACT_APP_STORAGE_PATH:
    NODE_ENV: production
  docker:
    web: Dockerfile
run:
  web: npm start